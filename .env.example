# Database Configuration
# Railway provides this automatically in production
# For local development, use your local PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/heartie

# JWT Secret for Authentication
# Generate a strong random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-here-change-in-production

# Node Environment
NODE_ENV=development

# Server Port (Railway sets this automatically in production)
PORT=3000
